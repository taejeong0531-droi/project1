# Firebase Admin 인증 설정(둘 중 하나 사용)
# 1) GOOGLE_APPLICATION_CREDENTIALS: 서비스 계정 키 JSON 파일 경로
# GOOGLE_APPLICATION_CREDENTIALS=/workspace/keys/firebase-sa.json

# 2) FIREBASE_CREDENTIALS_JSON: 서비스 계정 키 JSON 내용(문자열)
# FIREBASE_CREDENTIALS_JSON={"type":"service_account", ...}

# OpenWeatherMap API 키
OPENWEATHER_API_KEY=your_openweather_api_key

# 서버 포트(선택)
PORT=8080
